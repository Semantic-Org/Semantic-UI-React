general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 6

test:
  pre:
    - npm run lint
    - npm run tsd:lint
  post:
    - npm run build:commonjs
    - npm run tsd:test
    - bash <(curl -s https://codecov.io/bash)
