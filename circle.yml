machine:
  node:
    version: 4.2.1

dependencies:
  pre:
    - pip install awscli

test:
  post:
    - nvm install 5 && npm test

deployment:
  production:
    branch: master
    commands:
      - bash scripts/circle_deploy.sh
