general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 4.2.1

test:
  post:
    - nvm install 5 && npm test

deployment:
  production:
    branch: master
    commands:
      - bash scripts/circle_deploy.sh
