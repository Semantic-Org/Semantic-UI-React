import React, {Component} from 'react';
import {Button} from 'stardust';

export default class ActiveExample extends Component {
  render() {
    return (
      <Button className='active' />
    );
  }
}
