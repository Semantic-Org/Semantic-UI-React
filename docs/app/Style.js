const style = {};

style.container = {
  display: 'flex',
};

style.menu = {
  minWidth: 200,
  maxWidth: 250,
  flex: '1',
};

style.main = {
  minWidth: 400,
  maxWidth: 900,
  flex: '4 0 auto',
};

export default style;
