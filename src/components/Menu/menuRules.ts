import { rem } from '../../lib'

export default () => ({
  root: {
    display: 'flex',
    border: '1px solid rgba(34, 36, 38, .15)',
    borderRadius: rem(0.4),
    minHeight: rem(2.8),
    margin: 0,
    padding: 0,
    listStyleType: 'none',
  },
})
