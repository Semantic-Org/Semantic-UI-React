module.exports = (theme) => {
  const vars = {}

  /*-------------------
     Table Variables
  --------------------*/

  vars.headerBackground = `${theme.white}`
  vars.footerBackground = `${theme.white}`

  vars.cellVerticalPadding = `1em`
  vars.cellHorizontalPadding = `1em`

  vars.stateMarkerWidth = `1px`
  return vars
}
