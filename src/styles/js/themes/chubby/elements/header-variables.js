module.exports = (theme) => {
  const vars = {}

  /*-------------------
         Header
  --------------------*/

  vars.headerFont = `'Source Sans Pro', Helvetica Neue, Helvetica, Arial, sans-serif`
  vars.fontWeight = `bold`
  vars.textTransform = `none`

  /* HTML Headings */
  vars.h1 = `1.33rem`
  vars.h2 = `1.2rem`
  vars.h3 = `1rem`
  vars.h4 = `0.9rem`
  vars.h5 = `0.8rem`

  /* Sizing */
  vars.hugeFontSize = `1.33em`
  vars.largeFontSize = `1.2em`
  vars.mediumFontSize = `1em`
  vars.smallFontSize = `0.9em`
  vars.tinyFontSize = `0.8em`
  return vars
}
