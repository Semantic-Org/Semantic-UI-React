module.exports = (theme) => {
  const vars = {}

  /*******************************
              Sticky
  *******************************/

  vars.transitionDuration = `${theme.defaultDuration}`
  vars.transition = `none`
  vars.zIndex = `800`
  return vars
}
