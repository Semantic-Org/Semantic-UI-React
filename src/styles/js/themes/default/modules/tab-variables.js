module.exports = (theme) => {
  const vars = {}

  /*******************************
                Tab
  *******************************/

  /* Loading */
  vars.loadingMinHeight = `250px`
  vars.loadingContentPosition = `relative`
  vars.loadingContentOffset = `-10000px`

  vars.loaderDistanceFromTop = `100px`
  vars.loaderSize = `2.5em`
  return vars
}
