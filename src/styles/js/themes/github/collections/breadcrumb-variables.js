module.exports = (theme) => {
  const vars = {}

  /*******************************
           Site Overrides
  *******************************/

  vars.dividerOpacity = `1`
  vars.dividerSpacing = `0`
  vars.dividerSize = `${theme.big}`
  vars.dividerColor = `inherit`

  vars.huge = `1.5384em`


  return vars
}
