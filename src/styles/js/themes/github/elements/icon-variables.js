module.exports = (theme) => {
  const vars = {}

  vars.fontPath = `'../../themes/github/assets/fonts'`
  vars.fontName = `'octicons'`
  vars.fallbackSRC = `''`

  vars.width = `1em`
  vars.height = `1em`

  vars.small = `13px`
  vars.medium = `16px`
  vars.large = `18px`
  vars.big = `20px`
  vars.huge = `28px`
  vars.massive = `32px`
  return vars
}
