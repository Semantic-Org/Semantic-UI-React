module.exports = (theme) => {
  const vars = {}

  /*******************************
               Popup
  *******************************/


  vars.small = `${theme.relative10px}`
  vars.medium = `${theme.relative11px}`
  vars.large = `${theme.relative13px}`

  vars.verticalPadding = `${theme.relative7px}`
  vars.horizontalPadding = `${theme.relative11px}`


  return vars
}
