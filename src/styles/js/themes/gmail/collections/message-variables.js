module.exports = (theme) => {
  const vars = {}

  vars.background = `#F3F3F3`

  vars.boxShadow = `0px 0px 0px 1px rgba(0, 0, 0, 0.1) inset`
  vars.borderRadius = `4px`
  vars.verticalPadding = `7px`
  vars.horizontalPadding = `15px`

  vars.headerFontSize = `1em`

  vars.floatingBoxShadow = `0px 2px 4px rgba(0, 0, 0, 0.2)`

  vars.iconSize = `1.5em`
  vars.iconDistance = `1em`

  vars.warningBackgroundColor = `#F9EDBE`

  return vars
}
