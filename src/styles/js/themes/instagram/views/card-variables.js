module.exports = (theme) => {
  const vars = {}

  /*******************************
               Card
  *******************************/

  /*-------------------
           View
  --------------------*/

  vars.borderBoxShadow = `none`
  vars.shadowBoxShadow = `none`
  vars.boxShadow = `none`


  vars.internalBorderColor = `#EDEDEE`
  vars.border = `1px solid #EDEDEE`

  vars.contentPadding = `14px 20px`

  vars.metaColor = `#A5A7AA`

  vars.linkHoverRaiseDistance = `0px`
  vars.linkHoverBoxShadow = `none`
  vars.linkHoverBorder = `1px solid #D0D0D8`
  return vars
}
