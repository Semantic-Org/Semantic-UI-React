module.exports = (theme) => {
  const vars = {}

  /*-------------------
         Header
  --------------------*/

  vars.headerFont = `'Roboto', Arial, sans-serif`
  vars.fontWeight = `normal`

  vars.iconSize = `2em`
  vars.iconOffset = `0.2em`
  vars.iconAlignment = `top`

  vars.subHeaderFontSize = `1rem`


  /* HTML Headings */
  vars.h1 = `2.25rem`
  vars.h2 = `2rem`
  vars.h3 = `1.75rem`
  vars.h4 = `1.5rem`
  vars.h5 = `1.25rem`


  return vars
}
